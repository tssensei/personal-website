# Personal Website

Static starter portfolio for Ziteng Song.

## Local Preview

Open `index.html` in a browser, or serve the directory with any static server.

## Deploy

This can be deployed directly with Vercel as a static site.
